# Database connection (required)
DATABASE_URL=postgresql://user:password@localhost:5432/imberion

# CORS origins (comma-separated, defaults to http://localhost:3000)
CORS_ORIGINS=["http://localhost:3000"]

# App settings
DEBUG=true
